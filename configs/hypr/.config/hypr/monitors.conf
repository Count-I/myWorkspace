# Monitor configuration - uses auto-detection for portability
# Does NOT hardcode resolution/position (survives monitor swaps)

# Internal laptop display (HDMI-1 or eDP-1 depending on hardware)
monitor = eDP-1, preferred, auto, 1

# External monitors (wildcard for any additional monitors)
monitor = , preferred, auto, 1
